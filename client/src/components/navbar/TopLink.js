// @flow
import React from 'react';

type Props = {
    variant: string,
    className: any,
    text: string,
}

const TopLink = (props: Props) => {
    return (
        <div> hi</div>
    )
};

export default TopLink